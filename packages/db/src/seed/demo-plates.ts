import type { SeedDemoPlate } from "./types";

/**
 * 20 demo FR plates mapped to seeded vehicles (prototype brief §3.4).
 *
 * SPRINT 1 TODO: populate once `vehicles.ts` is filled — each plate's
 * `vehicle_slug` must match a `SeedVehicle.slug`.
 *
 * Format reminder: plates use the post-2009 SIV format "AB-123-CD".
 */
export const demoPlates: SeedDemoPlate[] = [];
