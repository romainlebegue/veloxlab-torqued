/**
 * Torqued shared UI primitives.
 *
 * Currently empty — apps/web owns its shadcn/ui components directly
 * (components/ui/*). This package exists for cross-app components
 * (web + future backoffice). Populate when backoffice is scaffolded.
 */

export {};
